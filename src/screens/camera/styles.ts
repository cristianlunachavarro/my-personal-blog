import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  buttonsContainer: {
    display: "flex",
    width: "100%",
    flexDirection: "row",
    alignItems: "center"
  },
  button: {
    width: "50%",
  },
});

export default styles;
